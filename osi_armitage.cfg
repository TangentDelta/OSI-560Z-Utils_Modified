MEMORY {
	RAM:
		start = $0400
		size = $2000;
	ROM:
		start = $E000
		size = $2000;
}

SEGMENTS {
	UTIL: load = RAM, type = rw, start = $0400;
	Z80IO: load = RAM, type = rw;
	I6100IO: load = RAM, type = rw;
}